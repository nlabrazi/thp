class SpecialSpell {
  constructor(dmg, mana, dmgCount, dmgBonus, hpBonus) {
    this.dmg = dmg;
    this.mana = mana;
    this.dmgCount = dmgCount;
    this.dmgBonus = dmgBonus;
    this.hpBonus = hpBonus;
  }
}

module.exports = SpecialSpell;
